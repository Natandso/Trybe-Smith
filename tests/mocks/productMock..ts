const validWeapon = 'Orin dagger'
const price = "200 peças de ouro"
const orderId = 5

const validRegister = {id: 7, name: validWeapon, price: price, orderId: orderId}
const existingUser = { 
    id: 7, 
    name: validWeapon,
    price: price,
    orderId: orderId,
  };


export default {
    validRegister,
    existingUser,
}